# 修改与提交记录

每完成一轮修改或收到一次平台反馈，都在这里追加一条记录。

---

## 第 0 轮：首次提交（2026-09-20）

- 提交内容：任务文件 5 张工作表、参考答案 8 张工作表、27 条评分规则拆分、32 条评分规则、两个模型的回复。
- 结果：**不通过**（2026-09-20 17:47，审核标签「其他」）。
- 退回原因（6 条实质意见）：任务文件疑似 AI 生成（表头颜色 + 内容）；参考答案单元格宽距错误；参考答案改动了源文件背景颜色；参考答案比源文件多出工作表；评分规则错误点未写出具体正确答案；评分规则高中分值占比过高。
- 本轮总结：见 `04-项目经验.md` 第二节。核心结论是「任务文件与参考答案必须由同一份脚本、同一套样式生成」，以及「错误点型评分规则必须写出正确答案」。

---

## 第 1 轮返修（进行中）

计划修改项与依据：

| # | 质检意见 | 修改方案 | 状态 |
| --- | --- | --- | --- |
| 1 | 任务文件疑似 AI 生成表格，需修改表头颜色并去除 AI 感 | 配色改为中性（标题纯文字、表头 `#E7E6E6` 浅灰、标签 `#F5F5F5`、待填/结果格 `#FFF7E6` 米色），删除「蓝色单元格为公式结果…」等说明句与「（数据已完整提供，无需外部检索）」等自述文字 | 已完成（工作簿） |
| 2 | 参考答案单元格宽距错误 | 每张工作表所有使用列显式设宽度，长表头行设置行高；自动检查「文字宽度 > 列宽且右侧有内容」为 0 处；任务文件与参考答案列宽逐列一致 | 已完成 |
| 3 | 参考答案修改了源文件的背景颜色 | 用同一份脚本、同一套格式生成两张表；逐格比对任务文件与参考答案的填充色，差异 0 处 | 已完成 |
| 4 | 参考答案比源文件多出工作表 | 任务文件补齐「月度收益」「行业风险归因」「风险回测」三张工作表（只保留表头/标签/日期结构，结果单元格留空），两张表工作表清单与顺序完全一致（8 张） | 已完成 |
| 5 | 评分规则错误点需写出具体正确答案 | 第 2/3/4 条扣分型考点重写，写明正确格式、正确图表数据源与正确保留范围；方案见 `06-评分规则-第2版.md` | 待平台录入 |
| 6 | 评分规则高中分值占比过高 | 高分值 11→7 条、低分值 4→7 条、中分值 15 条，总分仍 168；模型一 63.1%、模型二 50.6% | 待平台录入 |

### 第 1 轮返修：平台操作记录（2026-09-21）

| 步骤 | 操作 | 结果 |
| --- | --- | --- |
| 1 | 在平台解锁「任务文件」，删除旧文件并上传新版（28.5 KB） | 成功 |
| 2 | 在平台解锁「参考答案文件」，删除旧文件并上传新版（53.8 KB） | 成功 |
| 3 | 重新锁定两个文件并保存 | 成功 |
| 4 | 重新运行「参考答案文件检测」 | 仍判「未通过」，理由为多个指定区域"整体提前一行"；已填写不同意说明（检测器把表头行计入数据区） |
| 5 | 第 2、3、4 条固定扣分规则：由于规则正文不可编辑，把"正确答案"写进「证据来源/支持原因」字段 | 完成 |
| 6 | 第 32 条规则：正文补充标准答案（13.06% / 11.46% / 流动性冲击 -16.76% / 风控建议方向） | 完成 |
| 7 | 分值重排：13 条规则调整分值，高中低分布由 11/14/4 调整为 7/15/7，总分保持 168 | 完成 |
| 8 | 重新获取模型回复 | 进行中（平台提示约 15 分钟） |

### 第 1 轮返修：提交结果（2026-09-21）

| 项目 | 结果 |
| --- | --- |
| 模型回复重新生成 | 完成（模型一、模型二均产出新回复与新工作簿） |
| 评分规则覆盖度检查 | **通过**（补充规则 8 的 N 列基准月收益率考核、固定规则 2 的条件格式正确答案后，8 项评估维度全部满足） |
| 人机评一致率检测 | 检测 27 条未锁定规则，**不一致 0 条** |
| 模型一得分 | 104 / 168 分（61.9%），答出 20 / 32 条 |
| 模型二得分 | 106 / 168 分（63.1%），答出 22 / 32 条 |
| 难度预估勾选 | 模型一、模型二均勾选「得分 ≤ 65% = 是」 |
| 配额检查 | 通过 |
| 提交 | **提交成功** |

---

## 第 2 轮返修（2026-09-22）

### 平台反馈

- 第 2 次审核：**不通过**（2026-09-21 18:24），审核备注「辛苦根据备注进行修改」，剩余可返修次数 1。
- 新增意见：① 参考答案里被修改了背景色的区域超出 prompt 要求（prompt 只对 Alpha 三色、VaR 数据条、H4:H14 突破值提出颜色要求）；② 字体格式不一致；③ rubric 错误点要具体讲清模型的错误机制；④ 中分值占比偏高。
- 详细记录见 `docs/07-质检反馈-第2轮.md`。

### 本轮已完成

| 步骤 | 操作 | 结果 |
| --- | --- | --- |
| 1 | 重写 `build_workbooks.py` 的样式定义：删除全部 `bg_color`，只保留 H4:H14 突破值的条件格式底色 |
| 2 | 统一字体：所有单元格使用默认 Calibri 11，仅标题/分区/表头加粗 | 两张表字体只剩「Calibri 11 加粗」120 格、「Calibri 11 常规」4568 格 |
| 3 | 重新生成并自检 | 填充色差异 0 处、列宽差异 0 处、版面问题 0 处；参考答案 3562 格 / 1103 公式 / 3 图表 |
| 4 | 平台解锁并重新上传任务文件（28.4 KB）与参考答案（53.6 KB），重新锁定并保存 | 成功 |
| 5 | 重新运行参考答案文件检测 | 仍报「整体上移一行」，保留不同意说明 |
| 6 | 重新获取模型回复 | 进行中（约 15 分钟） |

### 待完成

1. 分值区间调整为 10/10/9/3，并保存。
2. 把 5 条规则（23、24、25、26、30）× 2 个模型的「未答出详情」错误原因改写为具体机制说明。
3. 模型回复完成后重新运行人机评一致率检测并核对得分率 ≤65%。
4. 重新提交。

### 当前阻塞（2026-09-22 上午）

工作簿与文件部分已全部完成；继续调整评分规则时，平台反复弹出「开启工作模式」录屏授权弹窗，
该弹窗会遮挡页面并拦截所有点击，导致分值与错误原因的自动修改无法落盘（每次都被弹窗阻断，
校验时数值仍为旧值）。需要人工在页面上点击一次「授权录屏并开始 / 继续计时」并保持页面在前台，
之后可继续完成：

- 分值：6→3、8→3、9→3、11→6、19→7、20→7、22→7、28→9、29→8（其余不变）
- 规则 23、24、25、26、30 的「未答出详情」错误原因改写

#### 本轮机评纠错要点

机器复核发现模型一在现金分红口径上采用了「月度含息收益连乘」，与题目要求的
`(当月收盘价 + 当月现金分红) / 上月收盘价 - 1` 不一致，因此规则 11、13、15、16
判定为未答出（例如 S001 累计收益 14.8849% 对应参考答案 14.7778%，Alpha -0.9685%
对应 -1.0208%，配置判断由「维持」纠正为「降低暴露」）。人工复核采纳该结论，
并按平台要求补齐了每条的「错误工作表名称 / 错误区域 / 错误原因」。

同时确认模型一在规则 28、29（风险回测滚动窗口与突破统计）数值与参考答案逐项一致，
人工判定由未答出改为答出。

#### 提交前校验清单（本轮实际执行）

1. 任务文件与参考答案工作表清单一致（8 张）。
2. 两张表逐格填充色差异 0 处、列宽差异 0 处。
3. 参考答案文件检测：AI 判定「未通过」（区域整体位移一行的误判），已填写不同意说明并保留解析器逐格核对结论。
4. 评分规则覆盖度检查：通过。
5. 人机评一致率检测：不一致 0 条。
6. 两个模型得分率均 ≤65%。
7. 提交后进入质检复核流程。

### 工作簿本轮改动明细

| 工作表 | 改动 |
| --- | --- |
| 原始行情 | 标题改为「模拟股票月度行情」；列宽按内容重设 |
| 参数设置 | 列宽重设（说明列 40） |
| 组合分析 | 标题改为「股票组合分析」，删除蓝色单元格说明行；P5 表头改为「95%单尾月度VaR贡献」；列宽与表头行高重设 |
| 月度收益 | 任务文件新增该表（日期 + 空白结果格） |
| 压力测试 | 标题改为「压力测试」，删除副标题说明；汇总区列宽与表头行高重设 |
| 投资仪表盘 | 标题改为「投资仪表盘」；区块标题改为「VaR贡献前五持仓」；表头改为「95%单尾月度VaR贡献」 |
| 行业风险归因 | 任务文件新增该表；标题改为「行业风险归因」 |
| 风险回测 | 任务文件新增该表；标题改为「风险回测」 |

### 校验结果

- 两张表工作表清单一致：原始行情、参数设置、组合分析、月度收益、压力测试、投资仪表盘、行业风险归因、风险回测（8 张）。
- 填充色差异 0 处、列宽差异 0 处。
- 版面自动检查：文字超出列宽且右侧有内容的单元格 0 处。
- 参考答案：3562 个带内容单元格、1103 个公式、3 张图表、0 个缓存错误；关键值仍为年化收益 13.06%、年化波动 11.46%、95% 单月 VaR ¥543,993。
- 任务文件：2460 个带内容单元格、0 张图表（图表由被测模型创建）。
